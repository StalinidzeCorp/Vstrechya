services:
  django: &django
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: backend
    command: gunicorn -c gunicorn_config.py
    volumes:
      - .:/usr/src/app
    ports:
      - "8010:8010"
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
      - POSTGRES_HOST
      - POSTGRES_PORT
      - EMAIL_USER
      - EMAIL_PASSWORD
      - SECRET_KEY
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
    depends_on:
      pgdb:
        condition: service_healthy

  pgdb:
    image: postgres
    restart: always
    env_file: .env
    environment:
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    container_name: pgdb
    volumes:
      - pgdbdata:/var/lib/postgresql/data/
    healthcheck:
      test: [ "CMD", "pg_isready", "-q", "-d", "$POSTGRES_DB", "-U", "$POSTGRES_USER" ]
      interval: 5s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"

  redis-qoovee:
    image: redis:6.0.9
    restart: always
    ports:
      - "6379:6379"

  celery-worker:
    build: .
    hostname: worker
    entrypoint: celery
    command: -A celery_app.app worker -n yatsworker --loglevel=info
    volumes:
      - .:/usr/src/app
    links:
      - redis-qoovee
    depends_on:
      - redis-qoovee
      - pgdb

  flower:
    build: .
    hostname: flower
    entrypoint: celery
    command: -A celery_app.app flower
    volumes:
      - .:/usr/src/app
    links:
      - redis-qoovee
    depends_on:
      - redis-qoovee
      - pgdb
    ports:
      - "5555:5555"

#  grafana:
#    image: grafana/grafana:latest
#    ports:
#      - "3333:3000"
#    volumes:
#      - grafana_data:/var/lib/grafana
#
#  prometheus:
#    image: prom/prometheus:latest
#    ports:
#      - "9090:9090"
#    command:
#      - '--config.file=/etc/prometheus/prometheus.yml'
#      - '--web.enable-lifecycle'
#    volumes:
#      - ./prometheus.yml:/etc/prometheus/prometheus.yml
#      - prometheus_data:/prometheus

volumes:
  pgdbdata: null
#  grafana_data:
#  prometheus_data: